---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: grafana-pvc
  namespace: monitoring  # Change to your preferred namespace
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 3Gi