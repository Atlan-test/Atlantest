apiVersion: v1
kind: Service
metadata:
  name: node-exporter
  namespace: monitoring  # Change to your preferred namespace
  labels:
    app: node-exporter
spec:
  ports:
    - port: 9090
      targetPort: 9090
      protocol: TCP
  selector:
    app: node-exporter