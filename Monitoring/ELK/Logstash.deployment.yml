apiVersion: apps/v1
kind: Deployment
metadata:
  name: logstash
  namespace: ecommerce
spec:
  replicas: 1
  selector:
    matchLabels:
      app: logstash
  template:
    metadata:
      labels:
        app: logstash
    spec:
      containers:
        - name: logstash
          image: docker.elastic.co/logstash/logstash:Latest
          ports:
            - containerPort: 5044

